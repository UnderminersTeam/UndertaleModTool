name: Build PR

on:
  pull_request:

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yml
    with:
      gui-single-file-config: "[ false ]"
      gui-bundled-config: "[ false ]"
      cli-bundled: "[ false ]"
