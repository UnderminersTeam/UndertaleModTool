<Window x:Class="UndertaleModTool.ProjectAssetsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UndertaleModTool"
        xmlns:loc="clr-namespace:UndertaleModTool.Localization"
        mc:Ignorable="d"
        Title="{loc:Loc Dialog_UnexportedProjectAssets}" Height="450" Width="800" IsVisibleChanged="Window_IsVisibleChanged" WindowStartupLocation="CenterOwner"
        Closing="Window_Closing"
        DataContext="{Binding Mode=OneWay, RelativeSource={RelativeSource Self}}">
    <Window.Resources>
        <local:ContextMenuDark x:Key="ListViewItemContextMenu">
            <MenuItem Header="{loc:Loc Common_Open}" Click="MenuItemOpen_Click"/>
            <MenuItem Header="{loc:Loc Common_OpenInNewTab}" Click="MenuItemOpenInNewTab_Click"/>
            <MenuItem Header="{loc:Loc ProjectAssets_UnmarkForExport}" Click="MenuItemUnmarkForExport_Click"/>
            <!--MenuItem Header="{loc:Loc Common_Copy}" Command="Copy"/-->
            <!--MenuItem Header="{loc:Loc Common_CopyAll}" Click="MenuItemCopyAll_Click"/-->
        </local:ContextMenuDark>
    </Window.Resources>
    <DockPanel>
        <Grid Margin="10,10,10,10" DragOver="Grid_DragOver" Drop="Grid_Drop" AllowDrop="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="{loc:Loc ProjectAssets_Description1}"/>
            <Label Grid.Row="1" Content="{loc:Loc ProjectAssets_Description2}"/>
            <ListView Grid.Row="2" Margin="0,8,0,0" ItemsSource="{x:Null}" MinHeight="300" x:Name="AssetsListView" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}">
                <ListView.CommandBindings>
                    <!--CommandBinding Command="Copy" Executed="CopyCommand_Executed"/-->
                </ListView.CommandBindings>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
                            </Trigger>
                        </Style.Triggers>
                        <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_MouseDoubleClick"/>
                        <EventSetter Event="KeyDown" Handler="ListViewItem_KeyDown"/>
                        <Setter Property="FontFamily" Value="Consolas"/>
                        <Setter Property="ContextMenu" Value="{StaticResource ListViewItemContextMenu}"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridView.ColumnHeaderContainerStyle>
                            <Style TargetType="{x:Type GridViewColumnHeader}">
                                <Setter Property="Foreground" Value="Black" />
                            </Style>
                        </GridView.ColumnHeaderContainerStyle>
                        <GridViewColumn Header="{loc:Loc ProjectAssets_NameColumn}" DisplayMemberBinding="{Binding Name}" Width="Auto"/>
                        <GridViewColumn Header="{loc:Loc ProjectAssets_AssetTypeColumn}" DisplayMemberBinding="{Binding AssetType}" Width="Auto"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DockPanel>
</Window>
